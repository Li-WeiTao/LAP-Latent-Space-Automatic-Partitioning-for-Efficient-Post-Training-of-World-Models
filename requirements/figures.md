# Figure environment

The checked-in figures were rendered with:

- R 4.6.1 (Windows UCRT)
- ggplot2 4.0.3
- a Cairo-enabled graphics device for PDF export

Exact package and platform details are recorded by the adjacent
`R_sessionInfo.txt` or `*_session_info.txt` file in each figure directory.
