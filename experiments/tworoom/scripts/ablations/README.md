# Ablation experiments

This directory contains manual-partition epoch sweeps, alternate horizons,
step-wise routing, and additional baseline variants. They are excluded from the
canonical main profile and listed by
`python experiments/tworoom/reproduce.py list --profile ablations`.
